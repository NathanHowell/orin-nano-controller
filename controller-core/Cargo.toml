[package]
name = "controller-core"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[features]
default = []
alloc = []

[dependencies]
logos = { workspace = true }
winnow = { workspace = true }
heapless = { workspace = true }
